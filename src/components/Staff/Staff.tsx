// Staff.tsx
import React from 'react';
import { Container, Text } from '@mantine/core';

const Staff: React.FC = () => {
  return (
    <Container size="lg">
      <Text size="xl"  >Staff Page</Text>
      {/* Add Staff content */}
    </Container>
  );
};

export default Staff;
